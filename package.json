{
  "name": "ark-asa-backup-web",
  "version": "1.0.6",
  "description": "ARK: Survival Ascended Backup Manager - Monorepo Root",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/schoolboyqueue/ark-asa-backup-web.git"
  },
  "author": "schoolboyqueue",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.12.0"
  },
  "scripts": {
    "prepare": "husky",
    "commit": "cz",
    "dev:client": "pnpm --filter ark-asa-backup-client run dev",
    "dev:server": "pnpm --filter ark-asa-backup-server run dev",
    "build:client": "pnpm --filter ark-asa-backup-client run build",
    "build:server": "pnpm --filter ark-asa-backup-server run build",
    "build": "pnpm run build:client && pnpm run build:server",
    "clean:client": "pnpm --filter ark-asa-backup-client exec rm -rf dist",
    "clean:server": "pnpm --filter ark-asa-backup-server exec rm -rf dist",
    "clean": "pnpm run clean:client && pnpm run clean:server",
    "lint:client": "pnpm --filter ark-asa-backup-client run format:check && pnpm --filter ark-asa-backup-client exec tsc --noEmit",
    "lint:server": "pnpm --filter ark-asa-backup-server run format:check && pnpm --filter ark-asa-backup-server exec tsc --noEmit",
    "lint:all": "pnpm run lint:client && pnpm run lint:server",
    "format:client": "pnpm --filter ark-asa-backup-client run format",
    "format:server": "pnpm --filter ark-asa-backup-server run format",
    "format:all": "pnpm run format:client && pnpm run format:server"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "@commitlint/config-workspace-scopes": "^20.0.0",
    "@commitlint/cz-commitlint": "^20.1.0",
    "commitizen": "^4.3.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.7.1"
  },
  "lint-staged": {
    "client/src/**/*.{ts,tsx,js,jsx,json,css}": "prettier --write",
    "server/src/**/*.{ts,js,json}": "prettier --write"
  },
  "packageManager": "pnpm@9.12.0"
}
